namespace Content.Shared._Funkystation.Medical.MedicalRecords;

public abstract class SharedMedicalRecordsSystem : EntitySystem // would entries be entities if they are only data?
{
    // for getting the information of each entry
    // probably also handle removing cata corpses from the job pool here
}
